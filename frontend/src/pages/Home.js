import React from 'react';

const Home = () => (
  <div>
    <h2>Welcome to User Management System</h2>
    <p>Use the navigation bar to create software, request access, and view pending requests.</p>
  </div>
);

export default Home;
